# TODO: Orchestrator MCP Server Template Stubs

**TO BE FILLED BY T15 (P4 orchestrator plugin — MCP server source)**

This directory will contain the TypeScript source for the `agntux-core` orchestrator's
local stdio MCP server. P6's plugin generator copies this tree and builds it when
generating the orchestrator plugin.

## Files T15 will deliver

Per P4 §6 (local stdio MCP server) and please-study-these-plans-fuzzy-valley.md §3.1:

```
canonical/mcp-server-templates/orchestrator/
├── package.json           # devDependencies: @modelcontextprotocol/sdk, typescript
│                          # engines.node: >= 18
├── tsconfig.json          # ESM target, strict, outDir: dist/
├── src/
│   ├── index.ts           # MCP server entrypoint — Server, StdioServerTransport,
│   │                      # ListResources, ReadResource, ListTools, CallTool handlers
│   │                      # (P4 §6.5 verbatim)
│   ├── ui-resources.ts    # resources/read ui://triage + ui://entity-browser handlers
│   │                      # attaches _meta.license (render_token) per P2a / P4.AMEND.1
│   │                      # (P4 §6.6 verbatim)
│   ├── s3-fetch.ts        # Signed-URL-aware S3 fetcher + on-disk LRU cache
│   │                      # exports readRenderTokenFromLicense() per P4.AMEND.1
│   │                      # (P4 §6.7 verbatim)
│   ├── csp.ts             # CSP _meta builder for MCP App host rendering
│   └── tools/
│       ├── snooze.ts      # Edits actions/{id}.md frontmatter: status → snoozed
│       ├── dismiss.ts     # Edits actions/{id}.md frontmatter: status → dismissed
│       ├── set-status.ts  # Edits actions/{id}.md frontmatter: status (general)
│       └── pivot.ts       # Emits host_prompt back to host to open an entity
└── dist/                  # Built JS — committed so users don't npm install
                           # (generated by tsc at plugin-build time, not pre-committed here)
```

## Placeholder variables

The orchestrator MCP server template carries one generator-substituted value:

| Placeholder | Substituted value | Where |
|---|---|---|
| `{{AGNTUX_APP_ID}}` | The orchestrator's AgntUX backend app ID | `.mcp.json` env block |

The TypeScript source files themselves carry no placeholders — they reference the
`signed_ui_base_url` from `~/.agntux/.license` at runtime (P4 §6.4 + §6.7).

## Do not add content here

Do not add source content to this directory until T15 lands. P6's generator reads
from this directory; placeholder content would be copied into production plugins.
