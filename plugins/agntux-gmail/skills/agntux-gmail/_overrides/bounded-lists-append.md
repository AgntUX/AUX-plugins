- `data/instructions/agntux-gmail.md → # Sender denylist` — last 30. Eviction: drop oldest entry whose HTML-comment metadata contains `added:` (auto-added). User-curated entries (no `added:` metadata) are never auto-evicted. Full auto-learn rules: see `reference/denylist.md`.
