{
  "plugin_slug": "agntux_slack",
  "plugin_version": "8.3.2",
  "handler_module": "view-tool/dist/agntux-slack-view.js",
  "view_tools": [
    {
      "name": "agntux_slack_compose_view",
      "description": "Open the Slack reply composer for an action. Pass action_id; the handler reads the action file's `## Compose payload` body section. Trigger phrases (host's tool selector matches the user's chat message against this list): `Use the agntux-slack plugin to open the reply composer for action {id}`, `Use the agntux-slack plugin to open the reply composer in schedule mode for action {id}`, `Use the agntux-slack plugin to draft a reply for action {id}`, `Use the agntux-slack plugin to draft a reply and schedule it for action {id}`. The legacy `ux: Use the agntux-slack plugin to …` envelope is still accepted for backwards compatibility with action items already on disk. This tool is an MCP App view tool: it returns a structured data payload that the host (Claude Desktop / Claude Cowork / Claude Code) renders into an interactive iframe shown above the next assistant turn. The iframe is the user-visible result of calling this tool; no additional chat output, summary, or visualization tool call is needed afterwards. After the user clicks Send / Schedule / Save Draft inside this iframe, the host invokes the Slack connector's send-message tool. The connector's success/error response is what the user needs next — surface it as plain chat text; the AgntUX view tool does not need to be re-invoked and the connector's own native UI does not need to be rendered.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "action_id": {
            "type": "string"
          }
        },
        "required": [
          "action_id"
        ],
        "additionalProperties": false
      },
      "outputSchema": {
        "type": "object",
        "properties": {
          "action_id": {
            "type": "string"
          },
          "initial_verb": {
            "type": "string"
          },
          "error": {
            "type": "string"
          }
        },
        "additionalProperties": true
      },
      "data_paths": [
        {
          "pattern": "actions/{id}.md",
          "scope": "personal"
        }
      ],
      "mcp_app_meta": {
        "resourceUri": "ui://agntux-slack/compose",
        "csp": {
          "connectDomains": [],
          "resourceDomains": [],
          "frameDomains": [],
          "baseUriDomains": []
        },
        "permissions": {}
      }
    },
    {
      "name": "agntux_slack_canvas_view",
      "description": "Open the Slack canvas summariser for an action. Pass action_id; the handler reads the action file's `## Canvas payload` body section. Trigger phrases (host's tool selector matches the user's chat message against this list): `Use the agntux-slack plugin to open the canvas summariser for action {id}`, `Use the agntux-slack plugin to summarise the thread for action {id}`. The legacy `ux: Use the agntux-slack plugin to summarise the thread for action {id}` envelope is still accepted for backwards compatibility with action items already on disk. This tool is an MCP App view tool: it returns a structured data payload that the host (Claude Desktop / Claude Cowork / Claude Code) renders into an interactive iframe shown above the next assistant turn. The iframe is the user-visible result of calling this tool; no additional chat output, summary, or visualization tool call is needed afterwards. After the user commits the canvas write from this iframe, the host invokes the Slack connector's canvas-create/update tool. The connector's success/error response is what the user needs next — surface it as plain chat text; the AgntUX view tool does not need to be re-invoked and the connector's own native UI does not need to be rendered.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "action_id": {
            "type": "string"
          }
        },
        "required": [
          "action_id"
        ],
        "additionalProperties": false
      },
      "outputSchema": {
        "type": "object",
        "properties": {
          "action_id": {
            "type": "string"
          },
          "error": {
            "type": "string"
          }
        },
        "additionalProperties": true
      },
      "data_paths": [
        {
          "pattern": "actions/{id}.md",
          "scope": "personal"
        }
      ],
      "mcp_app_meta": {
        "resourceUri": "ui://agntux-slack/canvas",
        "csp": {
          "connectDomains": [],
          "resourceDomains": [],
          "frameDomains": [],
          "baseUriDomains": []
        },
        "permissions": {}
      }
    }
  ],
  "ui_bundles": [
    {
      "uri": "ui://agntux-slack/compose",
      "html_path": "view-tool/dist/ui-resources/compose.html",
      "csp": {
        "connectDomains": [],
        "resourceDomains": [],
        "frameDomains": [],
        "baseUriDomains": []
      },
      "permissions": {}
    },
    {
      "uri": "ui://agntux-slack/canvas",
      "html_path": "view-tool/dist/ui-resources/canvas.html",
      "csp": {
        "connectDomains": [],
        "resourceDomains": [],
        "frameDomains": [],
        "baseUriDomains": []
      },
      "permissions": {}
    }
  ]
}
