{
  "name": "@agntux/build-mcp-server",
  "version": "0.2.0",
  "description": "Local stdio MCP server for the agntux-build plugin. Exposes a single tool — agntux_build_publish_to_team — that POSTs a built plugin tree to the team-private marketplace publish endpoint (S3.3).",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
