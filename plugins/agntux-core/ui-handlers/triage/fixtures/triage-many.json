{
  "args": { "limit": 50 },
  "prompt": "Call triage_view with arguments {\"limit\":50} and render the result.",
  "interactions": [
    { "type": "query", "selector": "[data-testid^='action-card-']", "max_results": 50 },
    { "type": "scroll_to", "selector": "[data-testid^='action-card-']:last-child" },
    { "type": "query", "selector": "[data-testid='handled-toggle']" }
  ],
  "_notes": "Wide-scope render: passes limit=50 (max). Exercises the inline scroll budget — the body must remain scrollable at the 600px iframe height with many rows. Also queries the handled-toggle accordion trigger to verify the recently-handled section renders."
}
